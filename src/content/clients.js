/**
 * Marquee wordmarks. Plain text rather than logo files on purpose — the row
 * has to read as one typographic band at the page's own weight, and a mixed
 * bag of vendor SVGs never does. Swap in real marks when they are cleared.
 */
export const clients = {
  caption: 'Regulated teams running agents in production on TORUK',
  names: [
    'HALDEN',
    'MERIDIAN',
    'NORTHBANK',
    'SEVERIN',
    'KAUFMANN',
    'ATLAS CIVIC',
    'ROSTAND',
    'VEYLAND',
  ],
}
